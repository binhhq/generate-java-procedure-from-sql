AND (v_id IS NULL OR link.ID = v_id)
      AND (v_code IS NULL OR req.CODE = v_code)
      AND (v_customerCode IS NULL OR c.CODE = v_customerCode)
      AND (v_fullName IS NULL OR c.FULLNAME = v_fullName)
      AND (v_phoneNumber IS NULL OR c.PHONE_NUMBER = v_phoneNumber)
      AND (v_code IS NULL OR req.CODE = v_code)
      AND (v_bankID IS NULL OR b.id = v_bankID)
      AND (v_cardNumber IS NULL OR pc.CARD_NUMBER = v_cardNumber OR li.MASK_INFO LIKE '%' || v_cardNumber || '%')
      AND (v_status IS NULL OR link.STATUS = v_status)
      AND (v_serviceCode IS NULL OR sb.CODE = v_serviceCode)
      AND (v_createdFrom IS NULL OR li.CREATED > v_createdFrom)
      AND (v_createdTo IS NULL OR li.CREATED < v_createdTo)
